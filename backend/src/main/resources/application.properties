# DB 

#why? 
spring.datasource.url=jdbc:mysql://localhost:3306/student_db?serverTimezone=UTC&useSSL=false
  #issue with timzone/resolved

#host ->3306 
#imp
spring.datasource.username=root
spring.datasource.password=Shivam@3141

# JPA / Hibernate Configuration
# update didnt wipe off DB, it just update Schema if needed 
spring.jpa.hibernate.ddl-auto=update
# remember to use vlaidate if depolying.
# never use create or create-drop :(
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=8080