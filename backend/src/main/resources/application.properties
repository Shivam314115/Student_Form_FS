# Database Connection Configuration
spring.datasource.url=jdbc:mysql://localhost:3306/student_db?serverTimezone=UTC&useSSL=false
spring.datasource.username=root
spring.datasource.password=Shivam@3141

# JPA / Hibernate Configuration
# 'update' is ideal for development; it keeps your tables without wiping them on every restart.
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# Server Port
server.port=8080